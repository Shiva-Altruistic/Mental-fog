const assert = require("assert");

function testResponse() {
  const response = "Take a small step and start with 5 minutes.";
  assert(response.length > 0);
}

testResponse();
console.log("Test passed!");